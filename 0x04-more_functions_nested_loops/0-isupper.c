#include "main.h"
/**
 * _isupper checks wether the letter is uppercase or not
 *
 * Return : equal 1 if ture and 0 if not
 */

int _isupper(int c)
{

	if (isupper(c))
		return 1;
	else
		return 0;
}
